---
// Stub: full implementation in Task 5
interface Props {
  prev?: { title: string; href: string };
  next?: { title: string; href: string };
}
const { prev, next } = Astro.props;
---
<div data-topic-nav data-topic-nav-stub></div>
